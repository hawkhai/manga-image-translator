# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 模块初始化 - 简化版本
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# import colorama                  # ❌ 不需要 - 仅用于彩色终端输出
# from dotenv import load_dotenv   # ❌ 不需要 - 仅用于加载 .env 环境变量

# colorama.init(autoreset=True)    # ❌ 不需要
# load_dotenv()                    # ❌ 不需要

# from .manga_translator import *  # ❌ 不需要 - 完整翻译系统，只需要 inpainting 模块
